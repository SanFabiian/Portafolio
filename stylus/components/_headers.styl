.header
	color color_first
	padding-left size_padding
	flex_position(flex, row, space-between, center)
	h1
		font-size font_size-title
.header__icon
	burgerNavIco(fixed, .1rem, alpha(color_first, .2),color_first)
	flex_position()
	i
		font-size font_size-title