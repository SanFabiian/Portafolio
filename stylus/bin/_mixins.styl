flex_position(dis = flex,direc = row,justi = center,align = center)
    display dis
    flex-direction direc
    justify-content justi
    align-items align

burgerNavIco(position, pTop = 0, bg = color_light,font_c = color_light)
    width size_icos
    height size_icos
    position position
    top pTop
    right 1rem
    background bg
    border-radius 50%
    color font_c

tone(dark = true)
    if dark
        background color_first
        color color_light